<!DOCTYPE html>
<html>
  <head>
    <title>LotteryApp</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/materialize/1.0.0/js/materialize.min.js"></script>
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>

    <% if notice %>
      <p class="alert alert-success"><%= notice %></p>
    <% end %>
    <% if alert %>
      <p class="alert alert-danger"><%= alert %></p>
    <% end %>

    <div class="navbar-fixed">
    <nav>
      <div class="nav-wrapper">
        <a href="/" class="brand-logo"
          ><i class="material-icons">Knowledge</i>Lottery</a
        >
        <ul id="nav-mobile" class="right hide-on-med-and-down">
          <% if user_signed_in? %> Logged in as
          <strong><%= current_user.email %></strong
          >. |
          <li>
            <%= link_to "My Lottery", user_lottery_index_path,
            :class => 'navbar-link' %>
          </li>
          <li>
            <%= link_to "Logout", destroy_user_session_path, method: :delete,
            :class => 'navbar-link' %>
          </li>
          <% else %>
          <li>
            <%= link_to "Sign up", new_user_registration_path, :class =>
            'navbar-link' %>
          </li>
          
          <li>
            <%= link_to "Login", new_user_session_path, :class => 'navbar-link'
            %>
          </li>
          <% end %>
        </ul>
      </div>
    </nav>
  </div>
    <div class="container">
    <div class="new badge col s12">
      <div class="new badge center card col s12">
       <div class="draw-start-notice"><h3> Lottery countdown starts</h1></div>
      </div>
    </div>
     <%= yield %>
    </div>
  </body>
</html>
